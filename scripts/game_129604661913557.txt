local addons = {}


local function load(Tab)
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local function findStringInTable(tbl, search)
    for _, value in ipairs(tbl) do
        if value == search then
            return true
        end
    end
    return false
end
	local timer = 0
	local timer2 = 0
	local autocollect = false
	local fastrespawn = false
	local afterrespawntp = false
	local autojoinonhopserver = false
	local autohopserver = false
	local waitminforhopserver = 2
	local tptoplayers = false
	local tptoplayersadd = "0,5,0"
	game:GetService("RunService").RenderStepped:Connect(function(dt)
		timer = timer + dt
		if autohopserver then
			if timer >= waitminforhopserver*60 then
    			local success, response = pcall(function()
    return game:GetService("HttpService"):JSONDecode(
        game:HttpGet("https://games.roblox.com/v1/games/129604661913557/servers/Public?sortOrder=Asc&limit=100")
    )
end)

local availableServers = {}

for _, server in ipairs(response.data) do
    if server.id ~= game.JobId and server.playing < server.maxPlayers and server.playing <= 6 then
        table.insert(availableServers, server.id)
    end
end


game:GetService("TeleportService"):TeleportToPlaceInstance(
    129604661913557,
    availableServers[math.random(1, #availableServers)],
    game:GetService("Players").LocalPlayer
)

			end
		else
			timer = 0
		end
	end)
	if not game:GetService("Players").LocalPlayer:GetAttribute("EnvironmentID") then
	task.spawn(function()
		wait(4)
		if autojoinonhopserver then
			game:GetService("ReplicatedStorage").Remotes.Arcade.Join:FireServer()
		end
	end)
end
	local tppos = "2641,5009,3323"
	local delaytptoplayers = 100
	local obrit = false
	local obritrange = 10000000
	local animenabled = false
	local animtype = "Gangnam"
	local animspeed = 1
	local function danceupdate(character)
	local animations = {
	Tornado = 135373056067761,
	LLLLLLL = 104639476409829,
	Gangnam = 78801539668900,
    SexyKarmelek = 102998462448180,
    AssSpin = 77094699387328,
    Spider = 113923275362771,
    Epstein = 111726465575659,
    Fish = 116819742540173,
    Chinaobrit = 80627968348042,
    Jugglehead = 130158622704929,
    HeadObrit = 136642332796314,
    Japko = 96207427647725,
    KoreaObrit = 102592263338924,
    Brazil = 83796130837213
}
	local humanoid = character:WaitForChild("Humanoid")
	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
	track:Stop()
end
	local animate = character:FindFirstChild("Animate")
	if animate then
		animate.Disabled = animenabled
	end
	if animenabled then
	local animation = Instance.new("Animation")
	animation.AnimationId = "rbxassetid://".. animations[animtype]
	local animTrack = humanoid:LoadAnimation(animation)
	animTrack.Priority = Enum.AnimationPriority.Action4
	animTrack.Looped = true
	animTrack:Play()
animTrack:AdjustSpeed(animspeed)
	else
		for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
	if track.Priority == Enum.AnimationPriority.Action4 then
		track:Stop()
	end
end
	end
end
player.CharacterAdded:Connect(function(c) task.wait(0.5) if animenabled then danceupdate(c) end end)
	Tab:CreateToggle({
		Name = "Auto Collect",
		CurrentValue = false,
		Flag = tostring(game.PlaceId) .. "/AutoCollect",
		Callback = function(Value)
			autocollect = Value
		end,
	})
	Tab:CreateToggle({
		Name = "Fast Respawn (can be bannable)",
		CurrentValue = false,
		Flag = tostring(game.PlaceId) .. "/FastRespawn",
		Callback = function(Value)
			fastrespawn = Value
		end,
	})
	Tab:CreateToggle({
		Name = "Auto Join On Hop Server (can be bannable)",
		CurrentValue = false,
		Flag = tostring(game.PlaceId) .. "/AutoJoinOnHopServer",
		Callback = function(Value)
			autojoinonhopserver = Value
		end,
	})
	
	Tab:CreateToggle({
		Name = "after respawn tp",
		CurrentValue = false,
		Flag = tostring(game.PlaceId) .. "/afterrespawntp",
		Callback = function(Value)
			afterrespawntp = Value
		end,
	})
	Tab:CreateToggle({
		Name = "tp to players (beta)",
		CurrentValue = false,
		Flag = tostring(game.PlaceId) .. "/tptoplayers",
		Callback = function(Value)
			tptoplayers = Value
		end,
	})

local Input2 = Tab:CreateInput({
   Name = "tp to players add",
   CurrentValue = "0,5,0",
   PlaceholderText = "0,0,0",
   RemoveTextAfterFocusLost = false,
   Flag = tostring(game.PlaceId) .. "/tptoplayersadd",
   Callback = function(Text)
		tptoplayersadd = Text
   end,
})
Tab:CreateSlider({
   Name = "Delay tp to players",
   Range = {1, 1000},
   Increment = 1,
   Suffix = "MS",
   CurrentValue = 100,
   Flag = tostring(game.PlaceId) .. "/delaytptoplayers",
   Callback = function(Value)
	delaytptoplayers = Value
   end,
})
local Input = Tab:CreateInput({
   Name = "tp pos",
   CurrentValue = "2641,5009,3323",
   PlaceholderText = "0,0,0",
   RemoveTextAfterFocusLost = false,
   Flag = tostring(game.PlaceId) .. "/tppos",
   Callback = function(Text)
		tppos = Text
   end,
})
Tab:CreateButton({
   Name = "set tp pos to body pos",
   Callback = function()
		local cf = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart.CFrame
		Input:Set(math.floor(cf.Position.X) .. "," .. math.floor(cf.Position.Y) .. "," .. math.floor(cf.Position.Z))
		tppos = math.floor(cf.Position.X) .. "," .. math.floor(cf.Position.Y) .. "," .. math.floor(cf.Position.Z)
   end,
})
local infjump = false
local jumpDoubleBoost = 1
Tab:CreateToggle({
		Name = "Inf Jump (7+)",
		CurrentValue = false,
		Flag = tostring(game.PlaceId) .. "/jumpinf",
		Callback = function(Value)
			infjump = Value
		end,
	})
Tab:CreateSlider({
   Name = "jump Double Boost",
   Range = {0.1, 10},
   Increment = 0.1,
   Suffix = "",
   CurrentValue = 1,
   Flag = tostring(game.PlaceId) .. "/jumpdoubleboost",
   Callback = function(Value)
	jumpDoubleBoost = Value
   end,
})
if getidentity() >= 7 then
local cc = require(game:GetService("Players").LocalPlayer.PlayerScripts.Controllers.MechanicsController)

local old=cc.DoubleJumpRequest;cc.DoubleJumpRequest = function(p93)
	if infjump then
        local v94 = p93.LocalFighter and p93.LocalFighter.EquippedItem
	if v94 then
		v94 = p93.LocalFighter.EquippedItem:Get("ObjectID")
	end
	if v94 and not p93.LocalFighter:IsGrounded() then
		p93:DoubleJump()
	end
    else
        return old(p93)
    end
end
cc.DoubleJump = function(p87)
	if p87:IsAlive() and not p87:IsFrozen() then
		local v88 = p87.LocalFighter:GetMoveVector(true, true)
		local v89 = p87.LocalFighter.Entity.RootPart
		local v90 = v88 * p87.LocalFighter.Entity.Humanoid.WalkSpeed * 1
		local v91 = (p87.LocalFighter.Entity.Humanoid.JumpPower * 1.1) * (infjump and jumpDoubleBoost or 1)
		v89.Velocity = v90 + Vector3.new(0, v91, 0)
		p87.LocalFighter.Entity:InstantlyRedirectCustomKnockback()
		p87:PlayMechanicsSound("DoubleJump")
	end
end
end
Tab:CreateToggle({
		Name = "obrit v2",
		CurrentValue = false,
		Flag = tostring(game.PlaceId) .. "/obrit",
		Callback = function(Value)
			obrit = Value
		end,
	})
Tab:CreateToggle({
		Name = "dance enabled",
		CurrentValue = false,
		Flag = tostring(game.PlaceId) .. "/danceenabled",
		Callback = function(Value)
			animenabled = Value
			danceupdate(game:GetService("Players").LocalPlayer.Character)
		end,
	})
	Tab:CreateDropdown({
   Name = "dance type",
   Options = {"LLLLLLL","Tornado","Gangnam","SexyKarmelek","AssSpin","Spider","Epstein","Fish","Chinaobrit","Jugglehead","HeadObrit","Japko","KoreaObrit","Brazil"},
   CurrentOption = {"Gangnam"},
   MultipleOptions = false,
   Flag = tostring(game.PlaceId) .. "/dancetype", 
   Callback = function(Options)
	animtype = Options[1]
	danceupdate(game:GetService("Players").LocalPlayer.Character)
   end,
})
Tab:CreateSlider({
   Name = "dance speed",
   Range = {0.25, 20},
   Increment = 0.25,
   Suffix = "",
   CurrentValue = 1,
   Flag = tostring(game.PlaceId) .. "/dancespeed",
   Callback = function(Value)
	animspeed = Value
    danceupdate(game:GetService("Players").LocalPlayer.Character)
   end,
})
	game:GetService("RunService").RenderStepped:Connect(function(dt)
		if obrit then
				for k, v in pairs(game:GetService("Players"):GetPlayers()) do
					if v.UserId ~= game:GetService("Players").LocalPlayer.UserId and v:GetAttribute("EnvironmentID") and v.Character:FindFirstChild("Humanoid") and v.Character.Humanoid.Health >= 1 and v:GetAttribute("SpectatingDuelObjectID") and game:GetService("Players").LocalPlayer:GetAttribute("SpectatingDuelObjectID") then
						pcall(function()
							game:GetService("Players").LocalPlayer.Character.HumanoidRootPart.CFrame = v.Character.HumanoidRootPart.CFrame * CFrame.new(math.random(-obritrange, obritrange),math.random(-obritrange, obritrange),obritrange)
						end)
						break
					end
				end
		end
	end)
	
local ShootAnim = false
local ReloadAnim = false
local RecoilAnim = false
local MuzzleFlash = false
local Bobbing = false
	Tab:CreateToggle({
		Name = "Delete ShootAnim (7+)",
		CurrentValue = false,
		Flag = tostring(game.PlaceId) .. "/delete1",
		Callback = function(Value)
			ShootAnim = Value
		end,
	})
Tab:CreateToggle({
		Name = "Delete ReloadAnim (7+)",
		CurrentValue = false,
		Flag = tostring(game.PlaceId) .. "/delete2",
		Callback = function(Value)
			ReloadAnim = Value
		end,
	})
Tab:CreateToggle({
		Name = "Delete RecoilAnim (7+)",
		CurrentValue = false,
		Flag = tostring(game.PlaceId) .. "/delete3",
		Callback = function(Value)
			RecoilAnim = Value
		end,
	})
Tab:CreateToggle({
		Name = "Delete MuzzleFlash (7+)",
		CurrentValue = false,
		Flag = tostring(game.PlaceId) .. "/delete4",
		Callback = function(Value)
			MuzzleFlash = Value
		end,
	})
Tab:CreateToggle({
		Name = "Delete Bobbing (7+)",
		CurrentValue = false,
		Flag = tostring(game.PlaceId) .. "/delete5",
		Callback = function(Value)
			Bobbing = Value
		end,
	})
if getidentity() >= 7 then
local cc = require(game:GetService("Players").LocalPlayer.PlayerScripts.Modules.ClientReplicatedClasses.ClientFighter.ClientItem.ClientViewModel)

cc.PlayAnimation = function(p43, animName, ...)
	if string.find(animName,"Shoot") and ShootAnim then
		return
	end
	if string.find(animName,"Reload") and ReloadAnim then
		return
	end
	return p43.Animator:PlayAnimation(animName, ...)
end
local old = cc.ApplyRecoil;cc.ApplyRecoil = function(...)
	if RecoilAnim then
		return
	else	
		return old(...)
	end
end
local old = cc.MuzzleFlash;cc.MuzzleFlash = function(...)
	if MuzzleFlash then
		return
	else	
		return old(...)
	end
end
local old = cc.Update;cc.Update = function(p100, p101, p102, p103)
	if Bobbing then
	p100._sway_spring.Value = Vector2.zero
p100._bobbing_value_spring.Value = Vector2.zero
p100._tilt_spring.Value = Vector2.zero
p100._jump_spring.Value = 0
p100._landing_spring.Value = 0
p100._recoil_spring.Value = Vector3.zero
p100._unrecoil_spring.Value = Vector3.zero
	end
	return old(p100, p101, p102, p103)
end
end
Tab:CreateToggle({
		Name = "Auto Hop Server",
		CurrentValue = false,
		Flag = tostring(game.PlaceId) .. "/AutoHopServer",
		Callback = function(Value)
			autohopserver = Value
		end,
	})
	Tab:CreateSlider({
   Name = "Wait Minute For Hop Server",
   Range = {1, 30},
   Increment = 1,
   Suffix = "M",
   CurrentValue = 2,
   Flag = tostring(game.PlaceId) .. "/WaitMinuteForHopServer",
   Callback = function(Value)
	waitminforhopserver = Value
   end,
})
Tab:CreateButton({
   Name = "Instant Hop Server",
   Callback = function()
local success, response = pcall(function()
    return game:GetService("HttpService"):JSONDecode(
        game:HttpGet("https://games.roblox.com/v1/games/129604661913557/servers/Public?sortOrder=Asc&limit=100")
    )
end)

local availableServers = {}

for _, server in ipairs(response.data) do
    if server.id ~= game.JobId and server.playing < server.maxPlayers and server.playing <= 6 then
        table.insert(availableServers, server.id)
    end
end

game:GetService("TeleportService"):TeleportToPlaceInstance(
    129604661913557,
    availableServers[math.random(1, #availableServers)],
    game:GetService("Players").LocalPlayer
)

   end,
})
    task.spawn(function()
	while task.wait() do
			if autocollect then
			for i, v in ipairs(game:GetService("Workspace"):GetChildren()) do
				if v.Name == "_drop" then
					pcall(function()
					firetouchinterest(game:GetService("Players").LocalPlayer.Character.HumanoidRootPart, v, 1)
					end)
					pcall(function()
					firetouchinterest(game:GetService("Players").LocalPlayer.Character.HumanoidRootPart, v, 0)
					end)
				end
			end
			end
            end
	end)
	pcall(function()
		game:GetService("Players").LocalPlayer.Character.Humanoid.Died:Connect(function()
			if fastrespawn then
				wait(0.25)
				game:GetService("ReplicatedStorage").Remotes.Duels.RespawnNow:FireServer()
			end
		end)
	end)
	game:GetService("Players").LocalPlayer.CharacterAdded:Connect(function(character)
        local humanoid = character:WaitForChild("Humanoid")
		task.spawn(function()
			if afterrespawntp then
				wait(0.5)
			local humanoidrootpart = character:WaitForChild("HumanoidRootPart")
			local x, y, z = tppos:match("([^,]+),([^,]+),([^,]+)")
			x, y, z = tonumber(x), tonumber(y), tonumber(z)
			local cf = CFrame.new(x, y, z)
			humanoidrootpart.CFrame = cf
			end
		end)
        humanoid.Died:Connect(function()
			if fastrespawn then
				wait(0.5)
				game:GetService("ReplicatedStorage").Remotes.Duels.RespawnNow:FireServer()
			end
        end)
    end)
	game:GetService("RunService").RenderStepped:Connect(function(dt)
		timer2 = timer2 + dt
		pcall(function()
			if tptoplayers then
				if timer2 >= delaytptoplayers/1000 then
					for k, v in pairs(game:GetService("Players"):GetPlayers()) do
						if v.UserId ~= game:GetService("Players").LocalPlayer.UserId and v:GetAttribute("EnvironmentID") and v.Character:FindFirstChild("Humanoid") and v.Character.Humanoid.Health >= 1 and v:GetAttribute("SpectatingDuelObjectID") and game:GetService("Players").LocalPlayer:GetAttribute("SpectatingDuelObjectID") then
							local x, y, z = tptoplayersadd:match("([^,]+),([^,]+),([^,]+)")
							x, y, z = tonumber(x), tonumber(y), tonumber(z)
							local cf = CFrame.new(x, y, z)
							pcall(function()
								game:GetService("Players").LocalPlayer.Character.HumanoidRootPart.CFrame = v.Character.HumanoidRootPart.CFrame * cf
							end)
							break

						end
					end
					timer2 = 0
				end
			else
				timer2 = 0
			end
		end)
	end)
end

addons.Load = load
return addons,415
--addons.Load(loadstring(game:HttpGet("https://raw.githubusercontent.com/men-cofing-roblox/Addons/refs/heads/main/TestScript.txt"))())
